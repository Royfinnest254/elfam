# Convex deployment URL (from `npx convex deploy` or the Convex dashboard).
# Must be set in Vercel → Settings → Environment Variables, then redeploy.
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Set automatically by Convex for auth; configure in the Convex dashboard.
CONVEX_SITE_URL=
