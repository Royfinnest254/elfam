# Memory Log — 2026-05-20

## What Happened
- Audited the application structure and verified Next.js 14 App Router + Convex + Tailwind CSS stack.
- Captured the exact color scheme from Tailwind config and verified visual design rules.
- Systematically removed all legacy Swiss Side blue hex codes (`#0052CC`, `#2684FF`, `#DEEBFF`) across the workspace and replaced them with Tailwind brand colors (`primary`, `primary-subtle`, `primary-dark`, `primary-light`). Modified files include:
  - `src/components/Wordmark.tsx`
  - `src/components/Sidebar.tsx`
  - `src/app/(manager)/herd/page.tsx`
  - `src/app/(manager)/manager/breeding/page.tsx`
  - `src/app/(manager)/manager/calves/page.tsx`
  - `src/app/(manager)/manager/calvings/page.tsx`
  - `src/app/(manager)/manager/incidents/page.tsx`
  - `src/app/(manager)/manager/inventory/page.tsx`
  - `src/app/(manager)/manager/inventory/flow/page.tsx`
  - `src/app/(manager)/settings/page.tsx`
  - `src/app/(worker)/layout.tsx`
  - `src/app/(worker)/worker/help/page.tsx`
  - `src/app/(worker)/worker/page.tsx`
  - `src/app/(worker)/worker/settings/page.tsx`
  - `src/app/signin/page.tsx`
  - `src/app/not-found.tsx`
  - `src/app/globals.css`
- Optimized performance by eliminating N+1 database queries:
  - Optimized `getHerdDashboard` and `getActiveWithholdings` queries in `convex/cows.ts` using Promise.all and pre-aggregations on date indexes.
  - Optimized `listInventoryMovements` query in `convex/records.ts` to perform a single batch lookup of users.
- Hardened database security, validations, and business logic:
  - Enforced a server-side withholding check in the `logMilkingSession` mutation, resolving active treatment statuses to prevent contaminated milk from entering the bulk tank.
  - Implemented server-side litres input validation to reject negative values.
  - Integrated the response warning payload on both manager and worker milking entry forms.
  - Removed the prohibited "Purge All Operational Data" UI button and code blocks from `/settings` page.
  - Removed a hardcoded welcome greeting on the manager dashboard, dynamically replacing it with the viewer's query name.
- Completed Generalization & Two-Role Migration Implementation:
  - Rebuilt the entire layout and routing flow with strict Manager/Worker segmentation.
  - Organized livestock registries, milking sessions, health/treatments, breeding, crops/fields, and inventory tracking under dynamic category structures.
  - Implemented mobile-first worker forms: Production logs, Health/Withholding treatments, Breeding logs, Crop activity logs, and Inventory movement / Proposal logs.
  - Created the manager-side approval interface for workers' proposed items and setting warning thresholds.
  - Developed a fleet logging component for maintenance logs with dynamic cost summaries and service type selectors.
  - Built an executive reports generator with printable layout views and PDF download functionality.
  - Added dedicated worker sub-pages for `/worker/equipment` (fleet tracking and maintenance logging) and `/worker/tasks` (checklists).
  - Developed a full Requests & Support tickets system, providing submission capability for workers at `/worker/requests` and validation queues (approve/reject/notes/delete) for managers at `/manager/requests`.
  - Fixed a TypeScript compilation error (TS2802) in `src/app/(manager)/manager/reports/page.tsx` by replacing the `Set` iterator spread operator with `Array.prototype.filter` to avoid requiring the `--downlevelIteration` flag in ES5 target.
  - Cleared unused rainfall logging variables and query triggers from `src/app/(manager)/manager/page.tsx`.
- Ran successful type checks (`npx tsc --noEmit`) and production bundling (`npm run build`) to ensure zero errors.

## Decisions Made
- Overrode the `flagged` field server-side in the milking session log mutation, returning warning payloads to the client without throwing Convex transactional errors. This preserves the audit log of the attempted entry.
- Completely removed the dangerous database purge button from the UI in settings page to align with the database safety rules.
- Adopted `text-primary-light` (teal `#00869B` in Tailwind config) as the active bottom nav item color for dark-navy mobile views in the worker portal.
- Standardized the database to be category-level to fit any mix of animals/crops dynamically without code rework.
- Used standard `filter` and `indexOf` array operations in Next.js page components to achieve array unique/deduplication rather than spreading ES6 `Set` constructors, ensuring broad browser compatibility and bypassing Next.js ES5 downleveling limits.

## Blockers Hit
- Encountered a TypeScript compilation error `TS2802` when building the production bundle. Fixed immediately by refactoring the `Set` iterator spread statement to standard Array filtering.

## Outputs Produced
- Modified `convex/records.ts`
- Modified `src/app/(manager)/manager/page.tsx`
- Modified `src/app/(manager)/manager/livestock/page.tsx`
- Modified `src/app/(manager)/manager/equipment/page.tsx`
- Modified `src/app/(manager)/manager/reports/page.tsx`
- Modified `src/app/(worker)/worker/record/livestock/page.tsx`
- Modified `src/app/(worker)/worker/record/inventory/page.tsx`
- Created `src/app/(worker)/worker/equipment/page.tsx`
- Created `src/app/(worker)/worker/tasks/page.tsx`
- Created `src/app/(worker)/worker/requests/page.tsx`
- Created `src/app/(manager)/manager/requests/page.tsx`
- Fixed a React runtime error (Rendered more hooks than during the previous render) in `src/app/(manager)/manager/crops/page.tsx`, `src/app/(manager)/manager/livestock/page.tsx`, and `src/app/(manager)/manager/inventory/page.tsx` by moving the delete mutation hooks above the early loading return statements.
- Updated walkthrough: [walkthrough.md](file:///C:/Users/roych/.gemini/antigravity/brain/d67e837d-e65d-441a-921a-5678a9a38b3a/walkthrough.md)
- Updated task list: [task.md](file:///C:/Users/roych/.gemini/antigravity/brain/d67e837d-e65d-441a-921a-5678a9a38b3a/task.md)
